apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kataklysmos-ring.fullname" . }}-test-connection"
  labels:
    {{- include "kataklysmos-ring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kataklysmos-ring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
